$('#userTable').DataTable({
    responsive: true,
    "columnDefs": [{
        // "targets": [4,5], // column or columns numbers
        // "orderable": false,  // set orderable for selected columns
    }],
    // "order": []
});